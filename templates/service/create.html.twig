{% extends 'base.html.twig' %}

{% block body %}

<h1>Création d'un service</h1>

    {{ form_start(form) }}
    {{ form_rest(form) }}
        <button>Ajouter</button>
    {{ form_end(form) }}

{% endblock %}