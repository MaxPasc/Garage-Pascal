{% extends 'base.html.twig' %}

{% block body %}

<h1>Création d'une question</h1>

    {{ form_start(form) }}
    {{ form_rest(form) }}
        <button>Ajouter</button>
    {{ form_end(form) }}

{% endblock %}